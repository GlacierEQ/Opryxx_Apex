# MEGA OPRYXX - Ultimate Recovery System Dependencies
# Combines all system requirements

# Core Python (Standard Library Only for Maximum Compatibility)
# No external dependencies required for core functionality

# Optional Enhanced Features (uncomment if needed):
# SQLAlchemy>=2.0.0          # Database operations
# alembic>=1.12.0            # Database migrations  
# aiohttp>=3.8.0             # Async HTTP for OAuth
# cryptography>=41.0.0       # Token encryption
# PyJWT>=2.8.0               # JWT tokens
# fastapi>=0.104.0           # Web API (if needed)
# uvicorn>=0.24.0            # ASGI server (if needed)

# Windows-specific (automatically installed on Windows):
# pywin32>=305               # Windows API access

# Development/Testing:
# pytest>=7.4.0             # Unit testing
# pytest-asyncio>=0.21.0    # Async testing

# Note: MEGA OPRYXX is designed to work with Python standard library only
# for maximum compatibility in recovery environments (Windows PE, etc.)
# Enhanced features are optional and can be enabled as needed.